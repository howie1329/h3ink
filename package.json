{
  "name": "h3ink",
  "private": true,
  "version": "0.1.0",
  "packageManager": "npm@11.6.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:desktop": "turbo run dev --filter=@h3ink/desktop",
    "dev:web": "turbo run dev --filter=@h3ink/web",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "thomas-agentkit": "^0.6.0",
    "turbo": "^2.5.0"
  }
}
